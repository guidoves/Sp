import { SexoDirective } from './sexo.directive';

describe('SexoDirective', () => {
  it('should create an instance', () => {
    const directive = new SexoDirective();
    expect(directive).toBeTruthy();
  });
});
